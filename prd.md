# VS Code 文件链接管理插件 - 产品需求文档 (PRD)

## 1. 项目概述

### 1.1 产品定位

一个 VS Code 扩展插件，用于在编辑器中快速管理和访问项目中的文件链接，支持通过菜单栏和右键菜单进行操作，提供便捷的文件导航功能。

### 1.2 核心价值

- 快速访问项目中的常用文件
- 支持文件链接的别名管理，提高可读性
- 支持目录分类和自定义排序，便于组织管理
- 配置基于项目存储，可跨项目复用

---

## 2. 功能需求

### 2.1 菜单栏集成

**功能描述：**

- 在 VS Code 顶部菜单栏添加插件操作入口
- 菜单项名称：建议为 "文件链接" 或 "File Links"
- 点击后显示文件链接管理面板/菜单

**交互方式：**

- 点击菜单栏入口，显示所有已添加的文件链接列表
- 支持点击链接快速打开对应文件
- 支持显示链接的别名（如果有设置）

---

### 2.2 文件区域右键菜单

**功能描述：**

- 在文件资源管理器（Explorer）的文件/文件夹上右键时，显示插件相关菜单项
- 菜单项名称：建议为 "添加到文件链接" 或 "Add to File Links"

**功能点：**

#### 2.2.1 添加文件链接

- 右键点击文件或文件夹，选择 "添加到文件链接"
- 弹出对话框/输入框，支持：
  - **文件路径**：自动填充当前选中文件的相对路径（基于项目根目录）
  - **别名设置**：可选，用户可输入自定义别名，如果不设置则使用文件名
  - **目录分类**：可选，用户可选择或创建目录分类，用于组织链接

#### 2.2.2 链接添加后的反馈

- 添加成功后，在菜单栏的插件入口中立即显示新添加的链接
- 提供成功提示（Toast 通知或状态栏提示）

---

### 2.3 菜单栏链接管理

**功能描述：**
在菜单栏的插件入口中，提供完整的链接管理功能。

#### 2.3.1 链接显示

- 以列表或树形结构显示所有已添加的文件链接
- 显示格式：`[别名] 或 [文件名] - [相对路径]`
- 支持点击链接快速打开文件

#### 2.3.2 自定义排序

- 支持拖拽排序（Drag & Drop）
- 支持手动调整顺序（上移/下移按钮）
- 排序后自动保存配置

#### 2.3.3 目录管理

- **创建目录**：支持创建自定义目录/分类
- **目录嵌套**：支持多级目录嵌套
- **移动链接**：支持将链接移动到不同目录
- **目录重命名**：支持重命名目录
- **目录删除**：支持删除目录（删除前需确认，可选择是否删除目录内的链接）

#### 2.3.4 链接编辑

- 支持编辑链接别名
- 支持编辑链接路径（如果文件移动了）
- 支持删除链接
- 支持在资源管理器中定位文件

---

### 2.4 项目级配置存储

**功能描述：**
所有配置和链接信息都基于单个项目存储，支持跨项目复用。

#### 2.4.1 配置文件位置

- 配置文件存储在项目根目录下
- 建议文件名：`.file-links.json` 或 `.vscode/file-links.json`
- 使用相对路径记录文件位置（基于项目根目录）

#### 2.4.2 配置数据结构

{
"version": "1.0.0",
"links": [
{
"id": "unique-id",
"path": "src/components/Button.tsx",
"alias": "按钮组件",
"category": "components",
"order": 0
}
],
"categories": [
{
"id": "category-id",
"name": "components",
"order": 0,
"parent": null
}
]
}

2.4.3 跨项目复用
支持导出配置文件（JSON 格式）
支持导入配置文件到其他项目
导入时自动验证文件路径是否存在
支持批量导入/导出 3. 使用场景
3.1 场景一：快速访问常用文件
开发者经常需要访问项目中的某些核心文件（如配置文件、工具函数等）
通过添加文件链接，可以在菜单栏快速访问，无需在文件树中查找
3.2 场景二：项目文档管理
在大型项目中，文档分散在不同目录
通过目录分类，将相关文档组织在一起，便于查找
3.3 场景三：团队协作
团队成员可以共享配置文件，统一项目中的常用文件链接
新成员加入项目时，导入配置文件即可快速了解项目结构 4. 技术实现要点
4.1 VS Code API 使用
菜单栏集成：使用 contributes.menus 配置菜单项
右键菜单：使用 contributes.menus/explorer/context 添加上下文菜单
命令注册：使用 contributes.commands 注册插件命令
文件操作：使用 vscode.workspace.fs API 进行文件读写
4.2 配置管理
使用 VS Code 的 workspace.fs API 读写配置文件
监听配置文件变化，自动刷新链接列表
支持配置文件格式验证
4.3 路径处理
使用相对路径存储（基于项目根目录）
打开文件时，将相对路径转换为绝对路径
处理路径不存在的情况（文件被删除或移动） 5. 用户交互流程
5.1 添加文件链接流程
用户在文件资源管理器中右键点击文件
选择 "添加到文件链接"
弹出输入对话框：
显示文件路径（只读）
输入别名（可选）
选择或创建目录（可选）
点击确认，链接添加到菜单栏
显示成功提示
5.2 管理链接流程
点击菜单栏插件入口
显示链接列表（支持目录折叠/展开）
用户可以：
点击链接打开文件
拖拽排序
右键菜单：编辑、删除、移动到目录
创建/编辑/删除目录
5.3 导入/导出配置流程
在插件设置或命令面板中选择 "导出配置"
选择保存位置，导出 JSON 文件
在其他项目中，选择 "导入配置"
选择配置文件，验证并导入
显示导入结果（成功/失败的文件路径） 6. 边界条件和限制
6.1 文件路径处理
如果文件被删除，链接仍然保留，但点击时提示文件不存在
支持文件重命名检测（通过文件系统监听）
支持相对路径和绝对路径的转换
6.2 性能考虑
大量链接时，使用虚拟滚动优化列表性能
配置文件大小限制（建议不超过 1MB）
链接数量限制（建议不超过 1000 个）
6.3 兼容性
支持 Windows、macOS、Linux
支持 VS Code 1.60.0 及以上版本
支持多工作区（Multi-root Workspace） 7. 未来扩展功能（可选）
7.1 高级功能
支持文件搜索和过滤
支持链接分组和标签
支持快捷键绑定
支持链接使用统计（最常访问的文件）
7.2 集成功能
与 Git 集成，支持按分支管理链接
与任务系统集成，支持快速打开任务相关文件
支持书签功能（在代码中标记位置）

功能完整性
[ ] 菜单栏可以正常显示插件入口
[ ] 右键菜单可以正常添加文件链接
[ ] 链接可以正常打开文件
[ ] 支持别名设置和显示
[ ] 支持目录创建和管理
[ ] 支持链接排序
[ ] 配置文件可以正常保存和读取
[ ] 支持配置导入/导出
8.2 用户体验
[ ] 操作流程顺畅，无明显卡顿
[ ] 错误提示清晰明确
[ ] 界面友好，符合 VS Code 设计规范
8.3 稳定性
[ ] 配置文件损坏时能正常处理
[ ] 文件不存在时能给出友好提示
